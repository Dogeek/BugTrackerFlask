Make the attachments thing be in a separate database, with either an issue id, or a comment id
each record has a link to the attachment, a unique ID, and the comment/issue id that the attachment
attaches to.

